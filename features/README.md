# Text Features

This directory is used to store the text features and anatomical description files required by SKMamba.

The actual feature files are not included in this GitHub repository. They will be released together with the ZMS dataset via Google Drive.

After downloading the released files, please place them in this directory:

```text
features/
├── zms_text_features.npy
└── descriptions.csv