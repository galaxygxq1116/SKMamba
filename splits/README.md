# Splits

Place `train.txt` and `test.txt` in this directory.

Each line should contain a relative image path. The class label is inferred from the parent directory name `A`, `B`, `C`, `D`, or `E`.

Example:

```text
A/0012 LL.bmp
A/0012 LU.bmp
A/0012 RL.bmp
A/0012 RU.bmp
```
